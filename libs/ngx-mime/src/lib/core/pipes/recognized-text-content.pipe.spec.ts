import { RecognizedTextContentPipe } from './recognized-text-content.pipe';

describe('RecognizedTextContentPipe', () => {
  it('create an instance', () => {
    const pipe = new RecognizedTextContentPipe();
    expect(pipe).toBeTruthy();
  });
});
